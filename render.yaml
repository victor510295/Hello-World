services:
  - type: web
    name: travel-audit
    runtime: python
    buildCommand: pip install -r travel_audit/requirements.txt
    startCommand: cd travel_audit && uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
